/** URL paths (English slugs for routing). */
export const PATHS = {
  root: '/',
  login: '/login',
  home: '/home',
  homeCourses: '/home/courses',
  homeMyCourses: '/home/my-courses',
  profile: '/profile',
} as const;
